<?php
class Stats_Bootstrap extends Zend_Application_Module_Bootstrap{
	    
     /*
     protected function _initDefaultController()
       {
           
           $front = Zend_Controller_Front::getInstance();       
           $front->addControllerDirectory(APPLICATION_PATH . '/modules/default/controllers');
           
           $config = new Zend_Config_Ini(APPLICATION_PATH . '/configs/routers.ini','product');
           //rewrite l?i url
           $router_rewrite = new Zend_Controller_Router_Rewrite();
         
           $router = $router_rewrite->addConfig($config, 'routes');
         
           							          							
           $front->setRouter($router);           
          	
           
           return $front;
           
        }
        */
        
}